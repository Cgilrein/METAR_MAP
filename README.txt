#  To install dependancies enter the following command...

chmod +x dependancies.sh

#  Afterwards, run the script by typing...

./dependancies.sh

#  This will automatically install needed libraries assuming you have an internet connection

#  Afterwards, to have the script begin automatically on every reboot, enter the following command

chmod +x begin.sh

#  Run the script with

./begin.sh